export const TRANSPORT_THRESHOLDS = Object.freeze({
  MAX_TEMP: 8.0,      // Maximum acceptable temperature in Celsius
  MIN_TEMP: 2.0,      // Minimum acceptable temperature in Celsius
  LOW_BATTERY: 20.0,  // Battery percentage below which an alert is triggered
  TAMPER_ENABLED: true, // Whether tamper alerts are enabled
});
