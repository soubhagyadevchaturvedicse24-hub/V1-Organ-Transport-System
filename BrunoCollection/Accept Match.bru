meta {
  name: Accept Match
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/matching/{{matchId}}/recipients/{{recipientId}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
