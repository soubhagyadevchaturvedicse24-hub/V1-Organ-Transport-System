meta {
  name: Generate Match
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/matching/organs/{{organId}}/run
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  if (res.status === 201) {
    bru.setEnvVar("matchId", res.body.data._id);
  }
}
